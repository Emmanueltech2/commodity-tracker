{
    "title": "Commodity Management",
    "icon": "area",
    "context": {
        "person": false,
           "place": true,
           "expression": "(!contact || contact.type === 'health_center') && user.parent.type != 'district_hospital' && !contact.muted"
        }
 }